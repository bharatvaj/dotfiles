mouse:
  hide_when_typing: true

import:
  - ~/.config/alacritty/theme.yml
  - ~/.config/alacritty/font.yml

cursor:
  style:
    shape: Beam

live_config_reload: true

shell:
  program: bash
  args:
    - --login
